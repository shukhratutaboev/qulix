@model IEnumerable<Company>
@{
    ViewData["Title"] = "Companies";
}


<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">Category List</h2>
		</div>
        <div class="col-6 text-end">
            <a asp-controller="Company" asp-action="Create" class="btn btn-primary">
               <i class="bi bi-plus-circle"></i> &nbsp; Create New Company
			</a>
		</div>
	</div>
    <br /><br />

<table class="table table-bordered table-striped" style="width:100%">
    <thead>
        <tr>
            <th>
                CompanyId
			</th>
            <th>
                Comapny Name
			</th>
            <th>
                Employees
			</th>
            <th></th>
		</tr>
	</thead>
    <tbody>
        @foreach(var obj in Model)
        {
            <tr>
                <td width="30%">
                    @obj.Id
				</td>
                <td width="30%">
                    @($"{obj.Name} {obj.OrganizationalForm}") 
				</td>
                <td width="20%">
                    @(obj.Employees is null ? 0 : obj.Employees.Count)
				</td>
                <td>
                    <div class="w-75 btn-group" role="group">
                        <a asp-controller="Dashboard" asp-action="CategoryEdit" asp-route-id="@obj.Id"
                        class="btn btn-primary mx-2"> <i class="bi bi-pencil-square"></i> Edit</a>
                        <a asp-controller="Dashboard" asp-action="CategoryDelete" asp-route-id="@obj.Id"
                        class="btn btn-danger mx-2"> <i class="bi bi-trash-fill"></i> Delete</a>
					</div>
				</td>
			</tr>
        }
    </tbody>

</table>
</div>